Args: $ARGUMENTS (day)

Use TodoWrite to create these todos:

[ ] Use the SlashCommand tool to invoke `/solve-part $ARGUMENTS 1`
[ ] Use the SlashCommand tool to invoke `/solve-part $ARGUMENTS 2`
[ ] Run `./scripts/save-conversation.sh $ARGUMENTS`
[ ] Reflect: Have you seen similar problems before? Name specific problems you've seen. Is this problem from a specific 'class' of problem you've seen? How did you come to your solution? Save response with `./scripts/save-response.sh $ARGUMENTS in-training-set.md "<response>"`
